# Cohort Analysis

Patterns for first-event cohorts, cohort sizing, and lifecycle order sequencing.

- `CA001` first order cohort per customer
- `CA002` cohort month acquisition counts
- `CA003` order status mix by cohort start
- `CA004` order sequence within customer cohort
