# Expressions

Patterns for scalar and grouping expressions that shape selected columns without
changing the root domain.

- `E001` CASE projection bucket
- `E002` COALESCE and NULLIF selectors
- `E003` distinct and statistical aggregates
- `E004` ROLLUP grouping helper
